<div style="padding:15px;background:#ccc;">
	<div class="row">
		<div class="col-md-12">
			<h1>Editing county</h1>
		</div>
	</div>

	<%= render 'form' %>

	<div class="row">
		<div class="col-md-1">
			<%= button_to 'Show', @county, { :class=> 'btn btn-primary', :method => 'get' } %>
		</div>
		<div class="col-md-1">
			<%= button_to 'Back', counties_path, { :class=> 'btn btn-default', :method => 'get' } %>
		</div>
	</div>
</div>
