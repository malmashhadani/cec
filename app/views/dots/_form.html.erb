<%= form_for(@dot) do |f| %>
  <% if @dot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dot.errors.count, "error") %> prohibited this dot from being saved:</h2>

      <ul>
      <% @dot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>
