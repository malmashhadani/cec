svg {
  background-color: white;
  display: block;
  margin: auto;
}
.background {
  fill: none;
  pointer-events: all;
}
.feature {
  cursor: pointer;
  stroke:black;
  stroke-width :0.6;
}
.feature.active {
  fill: #80E6FF;
  stroke-width :2;
}
.text {
	text-anchor: middle;
	font-size: 12px;
}
a:link,a:visited {
color: black;
}

div.tooltip {
    position: absolute;
    text-align: left;
    padding: 8px;
    font: 10px Verdana;
    background: none repeat scroll 0% 0% rgba(211, 215, 221, 1);
    border: px;
    border-radius: 8px;
    pointer-events: none;
    max-width: 360px;
    right: 40px;
    top: 40px;
    font-size: 20px;
    z-index: 10;
    opacity: 1;
}

